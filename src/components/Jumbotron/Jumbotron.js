import React from "react";
import "./Jumbotron.css";

const Jumbotron = () => (
	<header className = "header">
		<h1>Cats Clicky Game!</h1>
        <h2>Click the different images, but don't click the same one consecutively.</h2>
</header>
);

export default Jumbotron;